enum XColor {
  primary,
  secondary,
  error,
  warning,
  info,
  success,
  inheritText,
  inheritWhite,
  plain,
}

enum XButtonSize { small, medium, large }

enum XVariant { contained, outlined, text }

enum XColorType { success, warning, error, info, primary, secondary, plain }
