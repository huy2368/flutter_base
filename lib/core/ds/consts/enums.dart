enum DSButtonSize { small, medium, large }

enum DSVariant { elevated, outlined, text, filled, icon }

enum ColorType {
  success,
  warning,
  error,
  info,
  primary,
  secondary,
  inheritText,
  inheritWhite,
  plain,
}
